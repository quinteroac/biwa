{
  "goals": [
    "Provide a clear, configurable end-of-story screen consistent with the existing framework visual style.",
    "Let game developers trigger the end screen declaratively from the Ink script via a tag.",
    "Allow developers to set default end screen copy in `GameConfig`, keeping game-specific content out of framework code."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Player sees the end screen when the story reaches its conclusion",
      "description": "As a player, I want the end screen to appear when the Ink story emits the `# end_screen` tag so that I know the story has concluded.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "When the Ink script contains the tag `# end_screen`, `TagParser` produces a `TagCommand` with `type: \"end_screen\"`."
        },
        {
          "id": "US-001-AC02",
          "text": "`GameEngine` processes this command and emits an `\"end_screen\"` event on `EventBus`."
        },
        {
          "id": "US-001-AC03",
          "text": "`VnApp` listens for the `\"end_screen\"` event and transitions the UI state from `VnStage` to `VnEndScreen` (VnStage is unmounted)."
        },
        {
          "id": "US-001-AC04",
          "text": "`VnEndScreen` renders over a full-screen dark background with a title and optional message."
        },
        {
          "id": "US-001-AC05",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-001-AC06",
          "text": "Visually verified in browser: playing through a story to `# end_screen` shows the end screen."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Developer can configure the title and message shown on the end screen",
      "description": "As a game developer, I want to set the default end screen title and message in `GameConfig` so that each game can have custom end-of-story copy without modifying framework code.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`GameConfig` has an optional `endScreen?: { title?: string; message?: string }` field."
        },
        {
          "id": "US-002-AC02",
          "text": "`VnEndScreen` displays `GameConfig.endScreen.title` (fallback: `\"The End\"`) as the heading."
        },
        {
          "id": "US-002-AC03",
          "text": "`VnEndScreen` displays `GameConfig.endScreen.message` as a subtitle when provided; the element is absent when the field is omitted."
        },
        {
          "id": "US-002-AC04",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-002-AC05",
          "text": "Visually verified in browser: setting `endScreen.title` and `endScreen.message` in config renders correctly."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Player can return to the main menu from the end screen",
      "description": "As a player, I want a \"Return to Menu\" button on the end screen so that I can go back to the start menu after finishing the story.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`VnEndScreen` renders a \"Return to Menu\" button styled consistently with `VnStartMenu` buttons."
        },
        {
          "id": "US-003-AC02",
          "text": "Clicking \"Return to Menu\" unmounts `VnEndScreen` and re-renders `VnStartMenu` (engine state is reset so `hasSaves` reflects current saves)."
        },
        {
          "id": "US-003-AC03",
          "text": "The button is keyboard-focusable and activatable with Enter/Space."
        },
        {
          "id": "US-003-AC04",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-003-AC05",
          "text": "Visually verified in browser: clicking the button navigates back to `VnStartMenu`."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** `TagParser` must recognise the tag type `end_screen` and return a `TagCommand` with `type: \"end_screen\"`. Optional key-value fields (`title`, `message`) on the tag are forwarded as-is on the command object."
    },
    {
      "id": "FR-2",
      "description": "** `GameEngine` must handle a `TagCommand` of `type: \"end_screen\"` by emitting an `\"end_screen\"` event on `EventBus`, carrying an optional payload `{ title?: string; message?: string }`."
    },
    {
      "id": "FR-3",
      "description": "** `VnApp` must subscribe to the `\"end_screen\"` event on mount and transition to a new `\"ended\"` UI state when it fires."
    },
    {
      "id": "FR-4",
      "description": "** A new `VnEndScreen` component must be created at `framework/components/VnEndScreen.tsx`. It accepts props: `title: string`, `message?: string`, `onReturnToMenu: () => void`."
    },
    {
      "id": "FR-5",
      "description": "** `VnApp` must resolve the `title` shown on `VnEndScreen` by preferring the event payload, then `GameConfig.endScreen.title`, then the fallback string `\"The End\"`."
    },
    {
      "id": "FR-6",
      "description": "** The optional `endScreen` field must be added to the `GameConfig` interface in `framework/types/game-config.d.ts`."
    },
    {
      "id": "FR-7",
      "description": "** `VnEndScreen` visual style must match `VnStartMenu` — same dark gradient background, same accent colour variable (`--vn-accent`), same font variable (`--vn-font`)."
    }
  ]
}
